<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Подтверждение</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-dark-5@1.1.3/dist/css/bootstrap-dark.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to right, #edecec, #f43030);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .container {
            background: #fff;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 20px 35px rgba(0, 0, 1, 0.3);
            width: 400px;
            text-align: center;
        }

        .btn {
            background: #f43030;
            color: #fff;
            border: none;
            padding: 10px 20px;
            width: 100%;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 5px;
            cursor: pointer;
            transition: 0.9s;
        }

        .btn:hover {
            background: #ff8181;
        }

        input {
            width: 100%;
            padding: 0.7rem;
            margin: 1rem 0;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 1rem;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Подтвердите OTP</h1>
        <form method="post" action="/verify-otp">
            <input type="hidden" name="email" value="<%= email %>">
            <input type="text" name="otp" placeholder="Enter OTP" required>
            <button type="submit" class="btn">Подтвердить</button>
        </form>
        <form method="post" action="/resend-otp">
            <input type="hidden" name="email" value="<%= email %>">
            <button type="submit" class="btn" style="background: #ccc; color: #000;">Повторно отправить OTP</button>
        </form>
    </div>
</body>

</html>